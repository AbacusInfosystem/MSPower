@model MSPowerWebApp.Models.NewsLetterViewModel

@{
    ViewBag.Title = "NewsLetters";
}

@* News Letter Listing @Session["Language"].ToString();*@


@*<div class="lang-toggles">
    <select style="border:0px; outline:0px;" id="drpMainlang">
        @if (Session["Language"].ToString() == "en")
        {
            <option value="en" selected>English</option>
        }
        else
        {
            <option value="en">English</option>
        }

        @if (Session["Language"].ToString() == "ch")
        {
            <option value="ch" selected>Chinese</option>
        }
        else
        {
            <option value="ch">Chinese</option>
        }
    </select>
</div>*@

<div class="content">
    <div class="grid_4">
        <div class="container">
            <h1 class="blog_head">News Letters</h1>
            @{

                int i= 1;
            foreach (var item in Model.NewsLetters)
            {
                if(i==1 || i%2 ==0)
                {
        @:<div class="blog_grid span2">
                }
        <div class="col-md-6">
            <h3><a href="#">@item.NewsLetter_Title</a></h3>
            <div class="links">
                <ul>
                    <li><i class="fa blog-icon fa-calendar"> </i><span>@item.Updated_On.ToShortDateString()</span></li>
                </ul>
            </div>
            <p>
                @if (item.NewsLetter_Description.Length > 300)
                    {
                    @Html.Raw(item.NewsLetter_Description.Substring(0, 300)+ "...")
                    }
                    else
                    {
                    @Html.Raw(item.NewsLetter_Description)
                    }
            </p>
            @if (item.Language_Id == 1)
{
                        <a href="/en/news-letter?NewsLetter_Id=@item.NewsLetter_Id" class="btn btn-1 btn-1c">Read More</a>


}
            else
            {
                <a href="/ch/news-letter?NewsLetter_Id=@item.NewsLetter_Id" class="btn btn-1 btn-1c">Read More</a>
            }

        </div>

            if( i%2 ==0)
            {
        @:<div class="clearfix"> </div>
        @:</div>
        }
            i++;
        }

            }

        </div>
    </div>
</div>

    <script>
        $(function () {

            $(".top-menu").find("li").removeClass();

            $("#liNewsLetter").addClass("active");

        });
    </script>
